# External Secret para sincronizar secretos desde GCP Secret Manager
# Requiere External Secrets Operator instalado en el cluster
# Por ahora es un placeholder - se configurara cuando se instale ESO
apiVersion: v1
kind: ConfigMap
metadata:
  name: external-secrets-placeholder
  namespace: default
data:
  note: "External Secrets Operator se configurara aqui"
